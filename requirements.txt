# Native dependencies must use compatible wheels, never a cached local ABI build.
--only-binary=numpy,scipy,scikit-learn,pandas,numba,llvmlite,shap,xgboost,psycopg2-binary
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.30.6
websockets==17.1
psycopg2-binary==2.9.10
requests==2.34.2
xgboost==2.1.1
numpy==1.26.4
# U23: transitive native deps pinned to the versions RUNTIME_PREFLIGHT reports in
# production, so a rebuild cannot silently pick up a new ABI (preflight
# check_pins now enforces these too).
scipy==1.17.1
pandas==3.0.6
numba==0.67.0
llvmlite==0.49.0
yfinance==1.7.0
python-telegram-bot==21.5
httpx2==2.12.0
python-dotenv==1.2.3
pytz==2024.1
scikit-learn==1.5.2
vaderSentiment==3.3.2
shap==0.46.0
anthropic==1.8.0
