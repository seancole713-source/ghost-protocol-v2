# Test-only dependencies. Kept separate from requirements.txt so Railway
# does not pull pytest into the production runtime image.
pytest==9.1.1
pytest-asyncio==1.4.0
# config/settings.py (documentation-only, never imported at runtime) needs
# this to resolve for IDE type-checkers; production never installs it.
pydantic-settings==2.7.1
